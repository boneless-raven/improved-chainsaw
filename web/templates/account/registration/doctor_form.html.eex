<%= inputs_for @u, :doctor, fn d -> %>
  <div class="form-group">
    <%= label d, :hospital_id, class: "control-label" %>
    <%= select d, :hospital_id, Enum.map(@hospitals, &{&1.name, &1.id}), class: "form-control" %>
    <%= error_tag d, :hospital_id %>
  </div>
<%= end %>